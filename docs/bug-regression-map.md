ABOUTME: Tracks recurring bug classes with root causes and regression test coverage.
ABOUTME: Requires linking each bugfix to failing tests and acceptance evidence.

# Bug Regression Map

Use this map to prevent repeat regressions. Every bugfix should add or update an entry.

## Entry format

| Date (YYYY-MM-DD) | Symptom | Root cause (1 sentence) | Failing test name | Fix commit | Acceptance evidence links |
|---|---|---|---|---|---|
| 2026-03-03 | Edit -> Today mode switch opened mid-list after save | Shared `.screen-area` scroll position was not reset on mode change, so stale scroll offset persisted. | `resets screen-area scroll when switching between today and edit modes` | `0134ad4` | Before: `/tmp/weird-after-save-today.png`, After: `/tmp/weird-after-save-fixed.png` |
| 2026-03-03 | Top Today card appears clipped during scroll on mobile | `position: sticky` on `.today-active-day-header` over a touch-scrolling container caused overlay clipping artifacts. | `keeps today active-day header non-sticky to prevent clipping artifacts` | `e9030e4` | Before: `/tmp/workout-before.png`, After: `/tmp/workout-after.png` |
| 2026-03-03 | Renaming an exercise row can relink to another routine's exercise | Edit-save logic reused existing exercise ids by name, and duplicate-name merge/history linking collapsed intended isolation. | `creates an isolated exercise record when renaming to an existing exercise name` | `e9030e4` | Repro matrix: `/Users/seankim/code/workout/docs/mobile-ui-repro-matrix.md` |
| 2026-03-03 | Settings panels clipped on the right at mobile width | `.page` grid used implicit max-content sizing and native file input intrinsic width forced overflow in settings panel layout. | `settings page grid uses constrained single column track` | `pending` | Before: `/tmp/settings-before.png`, After: `/tmp/settings-after.png` |
| 2026-03-05 | After saving a set on iPhone, scrolling back to the Today/Edit toggle could get stuck | Vertical scrolling was still owned by a nested `.screen-area` under a body lock, so the document never became the stable scroll surface after the input/save transition. | `resets document scroll when switching modes` | `0aeb06d` | Before: `/tmp/reachability-before-390x844.png`, After: `/tmp/reachability-after-390x844.png` |
| 2026-03-06 | Bottom nav scrolls upward with Safari chrome while the routines list moves | The app used document scrolling with a viewport-fixed footer, so iOS visual viewport changes moved the nav instead of a shell-owned bottom row. | `uses screen-area as the vertical scroll owner` | `6497f95` | Before: `/tmp/nav-after-shell-reversal.png`, After: `/tmp/nav-after-shell-reversal-after-fix.png` |
| 2026-03-06 | Bottom nav can sit above the physical bottom edge in standalone iPhone mode | The app shell used `height: 100%`, which can under-fill the standalone iOS viewport and leave a dead strip below the nav row. | `uses dynamic viewport height for the app shell so the bottom nav reaches the standalone iOS screen edge` | `f7e6fb3` | Before: `/tmp/nav-regression-before-scroll.png`, After: `/tmp/nav-regression-after-dvh-fix.png` |
| 2026-03-06 | Bottom nav rises again after saving a set on iPhone | The app shell used `100dvh`, so save-time keyboard/browser viewport transitions could shrink the shell and lift the nav row. | `uses large viewport height for the app shell so save-time viewport changes do not lift the bottom nav` | `pending` | Before: `/tmp/nav-save-before-lvh-fix.png`, After: `/tmp/nav-save-after-lvh-fix.png` |
| 2026-03-07 | After the keyboard dismissed from `Save` or `Save routine`, the routines page could stay stuck instead of handing scroll back to the page | A nested `.screen-area` still owned vertical scrolling and the save path still carried inner-scroll preservation logic, so iOS never returned gesture control to the document after keyboard dismissal. | `resets the document scroll when saving a routine and changing modes` | `2ebb70b` | Before: `/tmp/document-shell-before-today.png`, `/tmp/document-shell-before-edit.png`; After: `/tmp/document-shell-after-today.png`, `/tmp/document-shell-after-edit.png` |
| 2026-03-07 | Bottom nav covers Today cards while scrolling the routines list | A sticky document footer keeps painting over the visible scroll region, so the page needs a dedicated shell row and an inner scroll owner to keep cards above the nav. | `docs/testing/repro-bottom-nav-overlap.js` | `d3626bc` | Before: `/tmp/bottom-nav-overlap-before.png`, After: `/tmp/bottom-nav-overlap-after.png` |
| 2026-03-08 | Bottom nav can be missing on initial load in installed iOS PWA mode | The shell fixed its height to `100lvh`, and on installed startup that can place the nav row below the visible viewport instead of inside the screen. | `uses dynamic viewport height for the app shell so the startup nav row stays inside the visible PWA viewport` | `37a6ef7` | Before: `/tmp/pwa-startup-nav-before.png`, After: `/tmp/pwa-startup-nav-dvh-after.png` |
| 2026-03-09 | Content shows through the bottom nav bar while scrolling the routines list | The footer surface used a translucent `rgba(..., 0.92)` fill, so cards visibly bled through the nav instead of reading as a solid shell row. | `docs/testing/repro-bottom-nav-opacity.js` | `bdaaec3` | Before: `/tmp/bottom-nav-opacity-before.png`, After: `/tmp/bottom-nav-opacity-after.png` |
| 2026-03-09 | Scrolled card content shows through the gap above the routines header | The scroll container owned the top padding, so the sticky header started `16px` below the scroll port and exposed live content in that strip while scrolling. | `docs/testing/repro-top-header-gap.js` | `612987c` | Before: `/tmp/top-header-gap-before.png`, After: `/tmp/top-header-gap-after.png` |
| 2026-03-10 | History sheet stops at five rows on exercises with more completed sessions | The history modal explicitly fetched `listExerciseHistory(exercise.id, 5)`, so older sessions never entered the sheet and there was nothing below row five to scroll to. | `returns every exercise history session when no limit is provided` | `890c189` | Before: `/tmp/history-sheet-before-fix.png`, After: `/tmp/history-sheet-after-fix.png`, Scrolled: `/tmp/history-sheet-scrolled-after-fix.png` |
| 2026-04-28 | Adding an exercise looks like it does nothing on mobile | The add action appended the draft row below the visible 390x844 viewport without revealing it, so the first successful add looked invisible. | `creates a new exercise record on the first add attempt when the text does not exactly match an existing exercise` | `eb3a2be` | Before: `/tmp/workout-add-failing-viewport.png`, After: `/tmp/workout-add-after-viewport.png` |
| 2026-04-28 | Unsaved routine edits disappear after switching to Today and back to Edit | Leaving edit mode cleared the routine hydration guard, so re-entering edit rebuilt drafts from the last saved routine and discarded staged rows. | `keeps unsaved exercise drafts when leaving and returning to edit mode` | `1864e48` | Before: `/tmp/workout-unsaved-before.png`, After: `/tmp/workout-unsaved-after.png` |
| 2026-04-28 | A newly saved exercise is added below the first Today viewport | Routine save switched to Today and reset the scroller to the top without revealing the exercise id that was added to the routine. | `reveals a newly added exercise after saving routine edits` | `1864e48` | Before: `/tmp/workout-saved-reveal-before.png`, After: `/tmp/workout-saved-reveal-after.png` |
| 2026-04-28 | Empty quick-entry validation appears above the visible viewport | The quick-entry validation path set only a top-level error while leaving the scrolled Today list at the lower card. | `reveals the error banner when a scrolled Today card is saved empty` | `1864e48` | Before: `/tmp/workout-empty-save-before.png`, After: `/tmp/workout-empty-save-after.png` |
| 2026-04-28 | Delete routine removes a routine without a confirmation step | The delete routine handler called the data-layer delete directly once more than one routine existed in the split. | `keeps the routine when delete confirmation is canceled` | `1864e48` | Before: `/tmp/workout-delete-cancel-before.png`, After: `/tmp/workout-delete-cancel-after.png` |
| 2026-04-28 | Edit-row exercise names are truncated on 390px screens | The edit-row header reserved a right-side grid column for all row actions, leaving too little width for the exercise title. | `docs/testing/repro-edit-row-title-width.js` | `1864e48` | Before: `/tmp/workout-edit-row-title-before.png`, After: `/tmp/workout-edit-row-title-after.png` |
| 2026-05-01 | Mobile scroll reachability repro cannot click the Today save action | The repro script still targeted the removed `.today-card__complete-button` selector after the card action became `.today-card__save-button`. | `docs/testing/repro-scroll-reachability.js` | `56ddbcd` | Before failure: `/tmp/reachability-before-stale-script.png`; After: `/tmp/reachability-before-script-fix.png`, `/tmp/reachability-after-script-fix.png` |
| 2026-05-12 | Import accepts exercises with invalid progression settings | Exercise import validation only checked that `progressionSettings` was an object, so impossible rep ranges and invalid numbers could be written. | `rejects imported exercises with invalid progression settings` | `ac6d466` | Failing test: `npm test -- src/lib/db.integration.test.ts`; Passing test: same command |
| 2026-05-12 | Import accepts routines that reference missing exercises | Routine import validation checked that `exerciseIds` were strings but did not verify that those exercise ids exist in the payload. | `rejects imported routines that reference missing exercises` | `ac6d466` | Failing test: `npm test -- src/lib/db.integration.test.ts`; Passing test: same command |
| 2026-05-12 | Import accepts set entries with missing parent records | Set-entry import validation checked field types but did not verify referenced session and exercise records exist. | `rejects imported set entries that reference missing parent records` | `ac6d466` | Failing test: `npm test -- src/lib/db.integration.test.ts`; Passing test: same command |

| 2026-09-22 | Reopening changes the session date or counts unfinished work as previous performance | Startup auto-completion and history predicates used current/default data rather than an explicitly finished session. | `resumes an unfinished session on another date without completing it`; `skips abandoned sessions and draft-only finished sessions when choosing previous performance` | `c4af7fe` | Full suite 161 passed; production finish/reload/history replay in companion verification bundle. |
| 2026-09-22 | Routine edits and bootstrap relink or overwrite historical exercise associations | Name-based repair and mutable routine membership stood in for stable exercise IDs and a session plan. | `keeps a session exercise plan separate from subsequent routine edits`; `keeps exact-name exercises separate when reading history or changing definitions` | `c4af7fe` | Database and rendered screen regressions; full suite 161 passed. |
| 2026-09-22 | Repeated taps or copied templates create false completed work | Completion lacked retry identity and template replacement treated historical/copy state as performed. | `retries the same completed set once and serializes concurrent set indexes`; `keeps completed work when applying a template and leaves copied sets incomplete` | `c4af7fe` | Double-tap production replay, including offline; database regressions. |
| 2026-09-22 | Correction saves can lose newer input or permit finishing before persistence | Debounce, pending writes, component transitions, and draft ownership were not coordinated. | `keeps finishing blocked until all edits made during a correction write are stored`; `resumes a correction stored before a reload instead of presenting stale saved work` | `c4af7fe` | Controlled pending-write tests, reload tests, browser correction replay; full suite 161 passed. |
| 2026-09-22 | Canceling can save on blur or replay a canceled correction after storage failure | Blur persisted before cancel; failed draft deletion was ignored. | `cancels a focused valid correction before blur can save it`; `keeps cancellation pending when its stored correction cannot be cleared` | `c4af7fe` | Failing-first fault injection then 14 component tests passed; full suite 161 passed. |
| 2026-09-22 | Historical edits lose precision or input when a sheet closes | Display-rounded strings and editor-local state were used as the correction source. | `preserves stored precision when opening and closing a correction`; `retains invalid and failed corrections when history is closed and reopened` | `c4af7fe` | History component regressions and close/reopen production replay. |
| 2026-09-22 | Unit changes reinterpret recorded weights or compare pounds with kilograms | Sets had no unit snapshot; calculations used current exercise defaults directly. | `migrates existing sets to preserve their interpreted unit without changing recorded values`; `compares loads in the configured unit before suggesting an increase` | `c4af7fe` | Disposable v2-to-v3 migration, export/import, unit and progression tests. |
| 2026-09-22 | Import leaves partial records/preferences or reapplies stale local drafts | Validation/write boundaries did not cover all associated local state. | `rolls back the imported records when saving their associated preferences fails`; `restores pending entry drafts and stored records when import encounters actual storage quota failure` | `c4af7fe` | Transaction and quota fault-injection tests; full suite 161 passed. |
| 2026-09-22 | Imported offset timestamps select the wrong previous workout | Lexical ISO timestamp ordering differs from chronological ordering when offsets differ. | `orders imported offset timestamps chronologically without changing stored dates`; `orders session dates by their actual time when recorded offsets differ` | `c4af7fe` | Both failed first, then 38 targeted tests passed; full suite 161 passed. |
| 2026-09-22 | Progression chooses the wrong set or silently treats missing/mixed work as success | Rule selected the first below-cap set and did not consistently filter completed unit-normalized work. | `targets the lowest last set and uses the first tied set without increasing load`; `ignores warmups, unfinished sets, and invalid completed values` | `c4af7fe` | Progression regressions and visible next-session explanation. |
| 2026-09-22 | Settings appear saved when device storage rejects them | Optimistic state changed before the preference write succeeded. | `keeps the applied preference and reports a full device instead of claiming a failed write applied` | `c4af7fe` | Actual quota failure regression; settings/theme browser replay. |

## Rules

1. Add one row per bug class; update existing rows when behavior changes.
2. Include the exact failing test name that captured the bug first.
3. Include the commit hash that fixed the bug.
4. Link acceptance evidence (logs/screenshots/PR artifacts).
